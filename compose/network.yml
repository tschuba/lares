# Docker network configuration for Lares
# This file defines the internal smart home integration network

networks:
  lares:
    driver: bridge
    name: lares
    ipam:
      config:
        - subnet: 172.20.0.0/16
