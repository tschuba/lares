services:
  luxtronik2mqtt:
    build:
      context: ../bridges/luxtronik
    container_name: luxtronik2mqtt
    restart: unless-stopped
    env_file:
      - ../config/.env
    environment:
      - MQTT_HOST=mosquitto
    networks:
      - lares

networks:
  lares:
    external: true
